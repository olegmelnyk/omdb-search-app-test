/*
 * MoviesSearch Messages
 *
 * This contains all the text for the MoviesSearch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MoviesSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MoviesSearch container!',
  },
});
