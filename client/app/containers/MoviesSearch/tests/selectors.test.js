// import { selectMoviesSearchDomain } from '../selectors';

describe('selectMoviesSearchDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
